AOS.init({
    duration: 1000,
    once: true
})

// tying effect 

const typed = new Typed("#typing",{
    strings:["UI/UX Designer.", "Web Developer.","Creative Thinker."],
    typeSpeed:60,
    backSpeed:30,
    backDelay:1000,
    loop:true,
})